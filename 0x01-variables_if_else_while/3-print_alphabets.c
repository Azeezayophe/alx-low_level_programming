#include<stdio.h>
#include<stdlib.h>
#include<time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int alp;
int alph;
for (alp = 97 ; alp <= 122 ; alp++)
{
putchar(alp);
}
for (alph = 65 ; alph <= 90 ; alph++)
{
putchar(alph);
}
{
putchar('\n');
}
return (0);
}
